---

---

<section class="hero">
  <div class="container hero-inner">
    <div class="hero-content">
      <span class="label reveal"
        >Ánalisis | Diseño | Desarrollo | Ejecución | Mantenimiento</span
      >
      <h1 class="heading-xl reveal reveal-delay-1">
        Hola! mi nombre es <span class="accent-word">Jhon Herbert</span>
      </h1>
      <div class="hero-actions reveal reveal-delay-3">
        <a
          href="https://www.linkedin.com/in/jhon-herbert-gonzales-hurtado-1b06a529a/"
          class="btn btn-ghost btn-large btn-linkedin"
          target="_blank"
        >
          <i class="fab fa-linkedin"></i>
          Linkedin
        </a>
        <a
          href="https://drive.google.com/file/d/1WX_KCro3HOxymlEf1lyFAscJ0OfohWN1/view?usp=sharing"
          class="btn btn-ghost btn-large btn-cv"
          target="_blank"
          ><i class="fas fa-file"></i> CV
        </a>
        <a
          href="https://github.com/JhonGonzzzales"
          class="btn btn-ghost btn-large btn-github"
          target="_blank"
        >
          <i class="fab fa-github"></i> GitHub
        </a>
      </div>
    </div>

    <div class="hero-visual reveal reveal-delay-2">
      <div class="shape-lima"></div>
      <div class="shape-violeta"></div>
      <div class="hero-logo">
        <img src="/h_logo.svg" alt="Herbert Diseño Gráfico" loading="eager" />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--space-xl) 0;
    min-height: calc(100vh - var(--header-height));
    display: flex;
    align-items: center;
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: var(--space-xl);
    align-items: center;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .accent-word {
    color: var(--accent);
    position: relative;
    display: inline-block;
  }

  .accent-word::after {
    content: "";
    position: absolute;
    bottom: 0.05em;
    left: 0;
    width: 100%;
    height: 0.15em;
    background: var(--accent);
    opacity: 0.4;
    border-radius: 2px;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-top: var(--space-sm);
  }

  .hero-visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 400px;
  }

  .hero-logo {
    position: relative;
    z-index: 2;
    max-width: 380px;
  }

  .hero-logo img {
    width: 100%;
    filter: drop-shadow(0 20px 40px rgba(51, 51, 51, 0.15));
  }

  .shape-lima {
    position: absolute;
    width: 280px;
    height: 280px;
    background: var(--accent);
    border-radius: 40% 60% 60% 40% / 60% 40% 60% 40%;
    opacity: 0.25;
    top: 10%;
    right: 10%;
    z-index: 0;
    animation: float 6s ease-in-out infinite;
  }

  .shape-violeta {
    position: absolute;
    width: 180px;
    height: 180px;
    background: var(--accent-secondary);
    border-radius: 50%;
    opacity: 0.15;
    bottom: 15%;
    left: 5%;
    z-index: 0;
    animation: float 8s ease-in-out infinite reverse;
  }

  /* —— Estilos de marca al hacer Hover —— */
  .btn-linkedin:hover {
    background: #0a66c2;
    border-color: #0a66c2;
    color: #ffffff;
  }

  .btn-github:hover {
    background: #24292e;
    border-color: #24292e;
    color: #ffffff;
  }

  :global([data-theme="dark"]) .btn-github:hover {
    background: #f0f6fc;
    border-color: #f0f6fc;
    color: #0d1117;
  }

  .btn-cv:hover {
    background: #ea4335;
    border-color: #ea4335;
    color: #ffffff;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-20px) rotate(5deg);
    }
  }

  @media (max-width: 900px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
      gap: var(--space-lg);
    }

    .hero-content {
      align-items: center;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-visual {
      min-height: 300px;
      order: -1;
    }

    .hero-logo {
      max-width: 260px;
    }

    .shape-lima {
      width: 200px;
      height: 200px;
    }

    .shape-violeta {
      width: 120px;
      height: 120px;
    }
  }

  @media (max-width: 480px) {
    .hero-actions {
      flex-direction: column;
      width: 100%;
    }

    .hero-actions .btn {
      width: 100%;
      max-width: 100%;
    }
  }
</style>
